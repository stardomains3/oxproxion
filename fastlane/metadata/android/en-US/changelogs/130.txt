- If the user prompt is large, the bubble will auto collapse. Tap the collapse button to see it fully.
- Added a collapse/uncollapse button to the AI bubble. This is manually done. Tap to collapse, tap again to uncollapse.
- Memory and compute optimizations for scrolling in a chat.